# ============================================================
# MLOps Mastery — Master Requirements
# Install all: pip install -r requirements.txt
# Or per-project: see each project's README
# ============================================================

# Core ML
scikit-learn>=1.3.0
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.11.0
matplotlib>=3.7.0
seaborn>=0.12.0

# Experiment Tracking (Project 1, 3)
mlflow>=2.7.0

# Data Versioning (Project 2)
dvc>=3.0.0
dvc-s3>=3.0.0          # AWS S3 remote
# dvc-gs               # Google Cloud Storage
# dvc-azure            # Azure Blob Storage

# Hyperparameter Optimization (Project 8)
optuna>=3.3.0

# Model Serving (Project 6)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.4.0
httpx>=0.25.0           # For testing FastAPI

# Monitoring (Project 7)
evidently>=0.4.0        # Industry-standard drift monitoring

# A/B Testing (Project 9)
statsmodels>=0.14.0     # For power analysis

# Pipeline Orchestration
apache-airflow>=2.7.0   # For Project 10 DAGs (heavy install — optional)
# prefect>=2.14.0       # Lighter alternative to Airflow

# Data Quality
great-expectations>=0.17.0   # Enterprise data validation

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0

# Code Quality
black>=23.0.0
flake8>=6.0.0
isort>=5.12.0
mypy>=1.5.0

# Database (for Feature Store)
sqlalchemy>=2.0.0

# Redis (for online feature store caching)
redis>=5.0.0

# Utilities
python-dotenv>=1.0.0    # Load .env files
pyyaml>=6.0.0           # Parse YAML configs
tqdm>=4.66.0            # Progress bars
joblib>=1.3.0           # Parallel processing
